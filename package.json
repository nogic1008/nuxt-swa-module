{
  "private": true,
  "workspaces": [
    "e2e/*",
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "lint": "eslint .",
    "prepare": "npm run prepare -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "devDependencies": {
    "@nuxt/eslint-config": "^0.1.1",
    "eslint": "^8.38.0"
  }
}
